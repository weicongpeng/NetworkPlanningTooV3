// MeasureControl - controls rendered on map via MapView WebView HTML
// This component returns null; measure controls appear at the last
// measurement point on the map as clickable overlay buttons.
// Button actions communicate back through postMessage/measureFinish/measureClear.
export default function MeasureControl() {
  return null;
}
