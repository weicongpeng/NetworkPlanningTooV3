---
phase: quick-260416
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/renderer/pages/MapPage.tsx
  - frontend/src/renderer/components/Map/LayerControl.tsx
  - frontend/src/renderer/locales/zh.json
  - frontend/src/renderer/locales/en.json
autonomous: true
requirements:
  - MAP-TOOL-OPT-01
  - MAP-TOOL-OPT-02
  - MAP-TOOL-OPT-03
  - MAP-TOOL-OPT-04
  - MAP-TOOL-OPT-05
  - MAP-TOOL-OPT-06

must_haves:
  truths:
    - "打点功能在装饰图层未勾选时无法激活，给出提示"
    - "装饰图层默认关闭"
    - "选择下拉菜单中圆形选择显示为圆形，多边形选择显示为多边形"
    - "选择下拉菜单的三个图标颜色与主菜单工具栏一致"
    - "框选/圆形/多边形都必须遵循唯一可见图层约束"
    - "搜索定位和地图搜索切换控件底色与同级菜单按钮一致"
    - "图层控制面板固定/隐藏控件使用无底色图钉图标，固定=绿色，隐藏=灰色，有悬停动画"
  artifacts:
    - path: "frontend/src/renderer/pages/MapPage.tsx"
      provides: "打点前置检查、选择下拉文案、框选唯一图层校验、搜索切换样式"
    - path: "frontend/src/renderer/components/Map/LayerControl.tsx"
      provides: "图钉图标样式改为无底色+绿色/灰色+悬停动画"
    - path: "frontend/src/renderer/locales/zh.json"
      provides: "中文翻译更新"
    - path: "frontend/src/renderer/locales/en.json"
      provides: "英文翻译更新"
  key_links:
    - from: "MapPage.tsx captureMode button onClick"
      to: "decorationLayer.visible check"
      via: "前置条件校验"
      pattern: "decorationLayer\\.visible"
    - from: "MapPage.tsx handleSelectionModeChange"
      to: "mode === 'point'"
      via: "唯一图层校验扩展到框选模式"
      pattern: "visibleCount.*point"
    - from: "LayerControl.tsx handleTogglePin"
      to: "图钉图标颜色切换"
      via: "isPinned state"
      pattern: "isPinned.*#22c55e|#6b7280"
---

<objective>
地图工具栏6项UI/逻辑优化：打点前置检查、装饰图层默认关闭、选择菜单文案和图标颜色统一、框选唯一图层约束、搜索切换按钮样式、图钉图标重设计。

Purpose: 提升地图工具的用户体验一致性，防止无效操作，统一视觉风格。
Output: 修改后的MapPage.tsx、LayerControl.tsx、zh.json、en.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- MapPage.tsx 关键状态和接口 -->

MapPage 组件关键 state:
```typescript
const [decorationLayer, setDecorationLayer] = useState<DecorationLayerOption>({
  id: 'decoration-layer',
  name: '装饰图层',
  visible: true,  // 需改为 false
  count: 0
})
const [captureMode, setCaptureMode] = useState(false)
const [selectionMode, setSelectionMode] = useState<'none' | 'circle' | 'polygon' | 'point'>('none')
const [searchMode, setSearchMode] = useState<SearchMode>('parameter')
```

DecorationLayerOption 接口 (LayerControl.tsx):
```typescript
export interface DecorationLayerOption {
  id: string
  name: string
  visible: boolean
  count?: number
}
```

当前框选逻辑 handleSelectionModeChange (MapPage.tsx ~line 364):
- 只有 circle 和 polygon 检查唯一图层约束
- point (框选) 没有检查 — 需要补上

当前搜索模式切换按钮 (MapPage.tsx ~line 1221):
- className="text-xs px-2 py-1 rounded bg-blue-400 text-white hover:bg-blue-500"
- 需改为与其他工具栏按钮一致的 muted 样式

当前图钉图标 (LayerControl.tsx ~line 1032):
- 有底色: isPinned 时蓝色背景 #3b82f6
- 需改为无底色，固定=绿色图标，隐藏=灰色图标，悬停有缩放动画

当前选择下拉菜单图标颜色:
- 框选: text-orange-500
- 圆形: text-blue-500 (CircleIcon className="text-blue-500")
- 多边形: text-green-500 (Pentagon className="text-green-500")
- 主菜单工具栏按钮: 无颜色class，使用 text-muted-foreground → 需要统一

现有 locale keys 需要修改:
- zh: "circleSelect": "圆形选择" → "圆形"
- zh: "polygonSelect": "多边形选择" → "多边形"
- en: "circleSelect": "Circle Select" → "Circle"
- en: "polygonSelect": "Polygon Select" → "Polygon"
- 需新增: "enableDecorationLayerFirst" 提醒用户先开启装饰图层
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 装饰图层默认关闭 + 打点前置检查 + 框选唯一图层约束 + 选择菜单文案图标 + 搜索切换样式</name>
  <files>frontend/src/renderer/pages/MapPage.tsx, frontend/src/renderer/locales/zh.json, frontend/src/renderer/locales/en.json</files>
  <action>
修改 MapPage.tsx 中以下内容（共5处改动）:

**改动1: 装饰图层默认关闭 (line ~157-162)**
将 `decorationLayer` state 初始值中 `visible: true` 改为 `visible: false`。

**改动2: 打点功能前置检查 — 必须勾选装饰图层 (line ~1308-1328)**
在"提取坐标"按钮的 onClick 处理中，激活 captureMode 之前添加检查：
```
if (newMode && !decorationLayer.visible) {
  alert(t('map.enableDecorationLayerFirst'))
  return
}
```
这确保用户未开启装饰图层时点击"打点"按钮，弹出 alert 提示而非激活打点模式。

**改动3: 框选(point)模式也检查唯一图层约束 (line ~364-395 handleSelectionModeChange)**
将条件 `if (mode === 'circle' || mode === 'polygon')` 改为 `if (mode === 'circle' || mode === 'polygon' || mode === 'point')`，使框选模式同样遵循唯一可见图层规则。

**改动4: 选择下拉菜单文案和图标颜色 (line ~1400-1424)**
下拉菜单内3个按钮的图标颜色都改为与主菜单工具栏一致（去掉 text-orange-500, text-blue-500, text-green-500，改为 text-muted-foreground）：
- 框选图标 svg: 去掉 `className="text-orange-500"`
- 圆形图标 CircleIcon: 改为 `className="text-muted-foreground"` (替换 text-blue-500)
- 多边形图标 Pentagon: 改为 `className="text-muted-foreground"` (替换 text-green-500)

下拉菜单中按钮文字使用翻译key更新:
- 框选: `{t('map.pointSelect') || '框选'}` — 保持不变
- 圆形: `{t('map.circleSelect') || '圆形'}` — 翻译key值改为"圆形"
- 多边形: `{t('map.polygonSelect') || '多边形'}` — 翻译key值改为"多边形"

**改动5: 搜索定位/地图搜索切换按钮底色 (line ~1221-1232)**
将搜索模式切换按钮的 className 从:
`"text-xs px-2 py-1 rounded bg-blue-400 text-white hover:bg-blue-500 transition-colors"`
改为与其他工具栏按钮（如打点、测距）未激活时一致的样式:
`"text-xs px-2 py-1 rounded text-muted-foreground hover:bg-muted hover:text-foreground transition-colors"`

**国际化 key 更新 (zh.json + en.json):**
- zh.json map 下: `"circleSelect": "圆形选择"` → `"circleSelect": "圆形"`
- zh.json map 下: `"polygonSelect": "多边形选择"` → `"polygonSelect": "多边形"`
- zh.json map 下新增: `"enableDecorationLayerFirst": "请先在图层控制面板勾选装饰图层再进行打点"`
- en.json map 下: `"circleSelect": "Circle Select"` → `"circleSelect": "Circle"`
- en.json map 下: `"polygonSelect": "Polygon Select"` → `"polygonSelect": "Polygon"`
- en.json map 下新增: `"enableDecorationLayerFirst": "Please enable Decoration Layer in layer control before using pin tool"`
  </action>
  <verify>
    <automated>cd D:/mycode/NetworkPlanningTooV3/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - decorationLayer.visible 初始值为 false
    - 点击"打点"按钮时若 decorationLayer.visible 为 false 则 alert 提示，不激活打点
    - handleSelectionModeChange 中 point 模式也检查唯一图层
    - 选择下拉菜单3个图标均为 text-muted-foreground 颜色
    - zh: circleSelect="圆形", polygonSelect="多边形"
    - en: circleSelect="Circle", polygonSelect="Polygon"
    - 搜索切换按钮使用 muted 样式，无蓝色背景
    - 新增 enableDecorationLayerFirst 翻译 key
  </done>
</task>

<task type="auto">
  <name>Task 2: 图层控制面板固定/隐藏控件图标重设计</name>
  <files>frontend/src/renderer/components/Map/LayerControl.tsx</files>
  <action>
修改 LayerControl.tsx 中固定/隐藏图钉控件（约 line 1032-1089）:

**改动: 图钉图标样式重设计**

将当前图钉按钮改为无底色、纯图钉图标样式，固定状态=绿色，隐藏状态=灰色:

1. 按钮本身去掉背景色变化:
```
backgroundColor: isPinned ? '#3b82f6' : (isPinHovered ? '#f3f4f6' : 'transparent'),
```
改为:
```
backgroundColor: 'transparent',
```

2. 图钉 SVG 颜色逻辑修改:
- 固定状态 (isPinned=true): 图钉整体使用绿色 (#22c55e)
- 隐藏状态 (isPinned=false): 图钉整体使用灰色 (#9ca3af)
- 悬停状态: 添加 transform 缩放动画

3. 具体 SVG 颜色修改:
- 图钉头部 circle:
  `fill={isPinned ? '#22c55e' : '#9ca3af'}`
  `stroke={isPinned ? '#16a34a' : '#d1d5db'}`
- 图钉针脚 path (d="M9 7.5 L9 14"):
  `stroke={isPinned ? '#22c55e' : '#9ca3af'}`
- 图钉底部 path (d="M6 14 L9 12.5 L12 14"):
  `stroke={isPinned ? '#22c55e' : '#9ca3af'}`

4. 添加悬停缩放动画效果:
在按钮的 style 中添加:
```
transform: isPinHovered ? 'scale(1.2)' : 'scale(1)',
transition: 'transform 0.2s ease, background-color 0.2s ease',
```

5. 更新 title 提示文本（支持 i18n）:
- 固定状态: title 改为提示"隐藏面板"的含义
- 隐藏状态: title 改为提示"固定面板"的含义
注意：当前 title 是硬编码中文，保持原样不改动翻译（快速任务范围内不做 i18n 改造）。
  </action>
  <verify>
    <automated>cd D:/mycode/NetworkPlanningTooV3/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - 图钉按钮无底色 (backgroundColor: transparent)
    - 固定状态图钉图标为绿色 (#22c55e)
    - 隐藏状态图钉图标为灰色 (#9ca3af)
    - 鼠标悬停时图钉图标有 scale(1.2) 缩放动画
    - 无蓝色背景残留
  </done>
</task>

</tasks>

<verification>
1. TypeScript 编译无错误: `cd frontend && npx tsc --noEmit`
2. ESLint 检查: `cd frontend && npm run lint`
</verification>

<success_criteria>
- 6项地图工具优化全部生效
- TypeScript 编译通过
- 所有新增/修改的 UI 文本使用 t() 翻译函数
- zh.json 和 en.json key 完全同步
</success_criteria>

<output>
After completion, create `.planning/quick/260416-x5t-map-tool-optimize/260416-x5t-SUMMARY.md`
</output>
