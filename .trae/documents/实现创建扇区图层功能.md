1. **添加创建扇区图层按钮**：在现网工参区域（create\_param\_update\_tab方法）添加一个"创建扇区图层"按钮，位于"更新工参"按钮旁边

2. **实现按钮点击事件**：创建`create_sector_layer`方法，处理按钮点击事件

3. **实现工参读取功能**：从全量工参文件中读取NR和LTE工参数据

4. **实现Table图层生成功能**：

   * 生成mapinfo可识别的table图层格式

   * 支持NR小区和LTE小区的图层生成

   * 包含必要的地理信息和小区属性

   * LTE和NR扇区图层分别制作

   * 添加图层制作自定义参数，用户可选制作扇区图层或点状图层。制作扇区图层时，扇区夹角也就是“波瓣宽度”（默认40度），扇区半径（默认60米），可自定义扇区颜色（默认蓝色）；制作点图层时默认小圆点（默认蓝色）即可

   * tab图层信息包括基站名称、小区名称、基站标识、PCI、TAC、经度、纬度、频点、小区ID、方位角、站高等关键信息

5. **保存图层文件**：将生成的图层文件保存到输出目录

6. **添加用户反馈**：在界面上显示图层生成的状态和结果

7. **测试功能**：确保生成的图层可以在mapinfo中正常打开

实现要点：

* 利用现有的全量工参文件读取功能

* 生成符合mapinfo table格式的文件

* 确保地理坐标转换正确

* 包含完整的小区属性信息

* 支持批量处理NR和LTE小区

