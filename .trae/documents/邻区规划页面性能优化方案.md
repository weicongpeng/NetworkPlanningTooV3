## 优化实施计划

### 修改1：延迟加载全量数据
- 修改 `loadFullParamsData` useEffect，移除页面进入时的自动执行
- 仅在规划结果存在且用户需要查看地图时才加载

### 修改2：使用缓存
- 将 `getMapData(12, true)` 改为 `getMapData(12, false)`
- 避免每次进入页面都强制刷新数据

### 修改3：异步初始化
- 将同步服务初始化改为不阻塞渲染
- 使用标志位控制后续操作

请确认后开始实施具体的代码修改。